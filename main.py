import tkinter as tk

root = tk.Tk()
root.title("Image Processing App")
root.geometry("800x600") 
label = tk.Label(root, text="Hello! Tkinter is working.", font=("Arial", 20))
label.pack(pady=50)

root.mainloop()
